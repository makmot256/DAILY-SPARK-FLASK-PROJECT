runtime: python312

entrypoint: gunicorn -b :$PORT app:app

instance_class: F1

automatic_scaling:
  min_instances: 0
  max_instances: 2
```

Your project structure should now look like this:
```
quote-app/
├── app.py
├── app.yaml          ← new file
├── requirements.txt
└── templates/
    └── index.html
```

---

## 2. Update `requirements.txt`

App Engine needs `gunicorn` to serve the app, so update your `requirements.txt` to:
```
flask
gunicorn